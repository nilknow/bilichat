package backend.tool;

import java.nio.charset.StandardCharsets;
import java.util.Arrays;
import java.util.Base64;

public class Password {
    /**
     * password public key encrypt
     */
    public static String encrypt(String password) {
        return "";

//        String url = "https://passport.bilibili.com/x/passport-login/web/key";
//        ApiClient.getClient().
//        String finalPasswd = password;
//        return Arrays.toString(Base64.getEncoder().encode(finalPasswd.getBytes(StandardCharsets.UTF_8)));
    }
}
